// Patch no longer needed: routing is handled at the application level.
// - lectoescritura.* host: src/pages/index.astro (SSR, Astro.rewrite)
// - lectoescritura.*/gracias: src/pages/gracias.astro (SSR, Astro.rewrite)
console.log('[patch] No patch needed — routing handled at app level');
