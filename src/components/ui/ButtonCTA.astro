---
const { href, class: className, ...props } = Astro.props
---

<a 
  href={href} 
  class={`inline-block bg-primary text-white font-medium rounded-md px-6 py-3.5 text-base hover:bg-cta-hover transition-colors duration-200 no-underline ${className}`}
  {...props}
>
  <slot />
</a>
